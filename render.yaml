# Render blueprint (optional — for separate API service later)
services:
  - type: web
    name: antionko
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    envVars:
      - key: HF_API_KEY
        sync: false
