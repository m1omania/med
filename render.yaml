# Render — один Web Service: Nuxt (SSR + API)
services:
  - type: web
    name: antionko
    env: node
    buildCommand: npm ci && npm run build
    startCommand: node .output/server/index.mjs
    envVars:
      - key: HF_API_KEY
        sync: false
